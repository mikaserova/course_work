<UserControl x:Class="course_app.Views.AddNewEmployee"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:course_app.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="326*"></RowDefinition>
            <RowDefinition Height="53*"></RowDefinition>


        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>
            <ColumnDefinition ></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <DataGrid Grid.ColumnSpan="3" ItemsSource="{Binding employee}" x:Name="emp_list" >
            
        </DataGrid>
        <Button  Grid.Row="1" Click="Button_Click">Add</Button>
        <Button  Grid.Row="1" Grid.Column="1" Click="Button_Click_2">Update</Button>
        <Button Grid.Column="2" Click="Button_Click_1" Height="64" Margin="0.4,0,0,-0.4" Grid.RowSpan="2" VerticalAlignment="Bottom">Delete</Button>
    </Grid>
</UserControl>
